#include "main.h"
/**
* _isupper(int c);
* @c: variable being checked
*
* Return: 1 if c is uppercase, 0 otherwise
*/
int _isupper(int c);
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
