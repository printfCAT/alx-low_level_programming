#include "main.h"
/**
* add - adds 2 integers and returns the result
* @a & @b are the integers to be added
* Return: result
*/
int add(int a, int b)
{
	int add = (a + b);
	_putchar(add);
	return (add);
}
