#include "main.h"
#include <stdio.h>
/**
* print_to_98 - prints all natural numbers up to 98
*/
void print_to_98(int n)
{
	int i;

	if (i = '0'; i <= '9'; i++)
	{
		putchar(i);
		putchar(',');
	}
		int c;

		if (c = '0'; c <= '8'; c++)
		{
			putchar(i);
			putchar(c);
			putchar(',');
		}
}
