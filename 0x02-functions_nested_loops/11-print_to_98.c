#include "main.h"
#include <stdio.h>
/**
* print_to_98 - prints all natural numbers up to 98
*/
void print_to_98(int n)
{
	int i;

	if (i = 48; i <= 57; i++)
	{
		putchar(i);
		putchar(',');
	}
		int n;

		if (n = 48; n <= 56; n++)
		{
			putchar(i);
			putchar(n);
			putchar(',');
		}
}
