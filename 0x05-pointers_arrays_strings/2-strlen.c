#include "main.h"
/**
* _strlen - returns the length of a string
* @s: string whose lenght is being checked
*
* Return: lenghth of string
*/
int _strlen(char *s)
{
	int len = 0;

	while (len != '\0')
		len++;
	return (len);
}
